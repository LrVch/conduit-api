{
  "name": "conduit-api",
  "version": "0.0.1",
  "description": "",
  "main": "dist/index.js",
  "devDependencies": {
    "@types/node": "^11.9.3",
    "nodemon": "^1.18.10",
    "ts-node": "^8.0.2",
    "typescript": "^3.3.3"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "nodemon --watch 'src/**/*.ts' --exec 'ts-node' src/index.ts",
    "build": "tsc -p .",
    "build:live": "nodemon --watch 'src/**/*.ts' --exec 'ts-node' dist/index.ts"
  },
  "author": "",
  "license": "ISC"
}
